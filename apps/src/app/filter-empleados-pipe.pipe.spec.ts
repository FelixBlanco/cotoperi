import { FilterEmpleadosPipePipe } from './filter-empleados-pipe.pipe';

describe('FilterEmpleadosPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmpleadosPipePipe();
    expect(pipe).toBeTruthy();
  });
});
