import { InputMonedaPipe } from './input-moneda.pipe';

describe('InputMonedaPipe', () => {
  it('create an instance', () => {
    const pipe = new InputMonedaPipe();
    expect(pipe).toBeTruthy();
  });
});
